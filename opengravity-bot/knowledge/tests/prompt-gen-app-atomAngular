Usos del agente:
/ai Qué es un DTO en Angular
/dev crear servicio HTTP tipado para pets
/explain [pega código]
/debug KeyError: 'user_id'
/review C:/ruta/a/archivo.ts
/analyze C:/ruta/a/carpeta
/angular C:/ruta/a/proyecto-angular
/swagger https://petstore.swagger.io/v2/swagger.json
/tests C:/backend/src/tests
/context search componente standalone
/create mi-app "Descripción de la app"


/create atom-petstore "Aplicación Angular ATOM para Petstore API:
- Componentes standalone con prefijo lib-
- Inyección vía inject(), estado con signal()/computed()
- ChangeDetectionStrategy.OnPush
- Estilos con tema M3: @use '@muface-lib/muface-lib/estilos/m3-theme'"

/create test-demo "App Angular ATOM minimalista con:
    - Un componente LibHelloComponent standalone
    - Muestra 'Hola ATOM' en un mat-card
    - Sin servicios, sin APIs, sin formularios
    - Solo valida que compila y corre en localhost:4200"

/create atom-ciudades "App ATOM con:
- LibCiudadesListComponent: tabla con datos mock
- LibCiudadesDetailComponent: detalle con mat-card
- Servicio CiudadesService con HttpClient
- Routing entre lista y detalle"


Ejemplo de mensaje en Telegram para crear una app Angular con web atom componentes:

/create atom-demo "Aplicación Angular corporativa ATOM para la API basada en C:/Users/pedrodulce/develop/generated/swagger-sample-app-demo.json

REGLAS OBLIGATORIAS ATOM:
• Componentes standalone con selector 'lib-' y clase 'Lib*'
• changeDetection: ChangeDetectionStrategy.OnPush
• Inyección de servicios vía inject(), no constructor
• Estado reactivo con signal()/computed()
• Lifecycle hooks: OnInit, AfterViewInit, OnDestroy
• Estilos en .scss separado, NO CSS inline
• Tema M3 global: @use '@angular/material' as mat; @use '@muface-lib/muface-lib/estilos/m3-theme' as muf-theme;

FUNCIONALIDADES REQUERIDAS:
• Listado de ciudades, organismos, expedientes, usuarios con búsqueda y filtrado
• Detalle de cada uno de esos elementos antes citados con edición
• Formulario reactivo con validación ATOM
• Servicio HTTP tipado con interceptors corporativoss
• Interfaces TypeScript para modelos de la API

GENERAR:
• LibCiudadesListComponent (standalone, OnPush, inject())
• LibCiudadService con HttpClient tipado
• Ciudad model interface desde Swagger
• Routing con lazy loading
• Estilos con variables de tema M3
• LibOrganismosListComponent (standalone, OnPush, inject())
• LibOrganismoService con HttpClient tipado
• Organismo model interface desde Swagger
• Routing con lazy loading
• Estilos con variables de tema M3
• LibExpedientesListComponent (standalone, OnPush, inject())
• LibExpedienteService con HttpClient tipado
• Expediente model interface desde Swagger
• Routing con lazy loading
• Estilos con variables de tema M3
• LibUsuariosListComponent (standalone, OnPush, inject())
• LibUsuarioService con HttpClient tipado
• Usuario model interface desde Swagger
• Routing con lazy loading
• Estilos con variables de tema M3"


# 1. ¿Hay un proceso node/ng corriendo?
Get-Process node, ng -ErrorAction SilentlyContinue | Select-Object Name, Id, CPU

# 2. ¿El puerto 4200 está en uso?
netstat -ano | Select-String ":4200"

# 3. ¿La carpeta de compilación está creciendo?
Get-ChildItem "C:/Users/pedrodulce/develop/generated/test-demo/.angular" -Recurse -Directory -ErrorAction SilentlyContinue | Measure-Object

# 4. Probar conexión HTTP (puede fallar mientras compila)
Invoke-WebRequest -Uri "http://localhost:4200" -UseBasicParsing -TimeoutSec 2 -ErrorAction SilentlyContinue

# 1. Ir a la carpeta del proyecto generado
cd C:/Users/pedrodulce/develop/generated/test-demo

# 2. Detener el proceso node que está bloqueado (opcional, pero recomendado)
Stop-Process -Id 13296 -Force -ErrorAction SilentlyContinue

# 3. Ejecutar ng serve MANUALMENTE para ver output completo
ng serve --port 4200 --host 0.0.0.0 --disable-host-check

# 👀 Observa el output completo:
# - Si hay errores de compilación, aparecerán aquí
# - Si dice "Compiled successfully", la app funcionará
# - Si se queda "colgado" de nuevo, el problema es más profundo

# Verificar que @angular/material está instalado
npm list @angular/material @angular/cdk

# Si no aparece o hay errores, reinstalar:
npm install @angular/material@^19.0.0 @angular/cdk@^19.0.0 --legacy-peer-deps



Esto pasa porque `fallback_theme` se define dentro de un `if`, pero se usa fuera de ese bloque.

---

## ✅ Solución #1: Regex que soporta CON y SIN bloques markdown

**Reemplaza `_parse_and_write_files` con esta versión más flexible:**

